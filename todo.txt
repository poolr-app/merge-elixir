TODO:

    proper use of all enums
        EG:
            is_admin -> PermissionLevel enum
    
    setup 2fa codes 
    do full ActionScript Queries for where clauses
    fix token generation (comeonin) and validation
        then ensure the 
    do seeding of locations and trips
    model relations
        then
            the created trips table
            the rejected trips table
            the cancelled trips table
    invites model and controller

    tests!
